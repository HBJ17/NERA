services:
  nera-platform:
    build: .
    image: nera-platform:latest
    container_name: nera_app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - DEBUG=False
    restart: unless-stopped
